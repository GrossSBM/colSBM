future_is_installed <- TRUE # ("future.apply" %in% installed.packages())

os_is_windows <- FALSE # (toupper(Sys.info()["sysname"]) == "WINDOWS")

parallel_is_installed <- FALSE # ("parallel" %in% installed.packages())

M <- 5
